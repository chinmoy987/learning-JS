"use strict"; // treat all Js code as newer versions

//alert(3 + 3)    // we are using nodeJs,not browser

console.log(
  3 + 3 // always maintain high code readablity,so don't do this
);

let fullName = "chinmoy";
let age = 20;
let isLogedIn = false;

// Primitive datatypes--------------------------------------------------------------------------------
// number=> 2 to the power 53 is the range
// bigint=> for bigger numbers
// for strings => " "
// boolean => true/false
// null => standalone value (empty) (datatype is object)
// undefing => value is not initialized yet
// symbol => unique

console.log(typeof fullName);